package authenticator.src.com.authenticator;

public class TOTPStrategy implements TokenGenerationStrategy{
    
    @Override
    public String generateToken(String secret){

        return null;
    }
}